# frozen_string_literal: true

# ractor-tvar is a compatibility shim: the implementation lives in the
# ractor-sharing gem, which this gem depends on.
require "ractor/tvar"
require "ractor/tvar/version"
